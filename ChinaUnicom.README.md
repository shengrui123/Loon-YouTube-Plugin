# 中国联通净化 · Loon

插件：[ChinaUnicom.Clean.Loon.plugin](./ChinaUnicom.Clean.Loon.plugin)。使用 Loon 原生 Rewrite 与内联 JQ，单文件，无远程 JavaScript 依赖。

## 安装

1. 使用 Loon 3.5.1（978）或更高版本；建议更新到当前可用最新版。此插件只用新版 Rewrite，不使用需要 983 的新版 Script。
2. 在 Loon 的插件管理中导入该 `.plugin` 文件。若使用远程订阅，先将文件托管到你自己的 HTTPS 直链，再添加该地址；本次没有发布到网络。
3. 开启插件、复写与 MitM；安装 Loon 的 CA 证书，并在系统证书信任设置中启用完全信任。
4. 停用其他中国联通净化插件，完全退出联通 App 后重新打开。

## 默认效果与开关

- 拦截 `ad.10010.com` 及其子域名，屏蔽已知营销、新人专享和首页瀑布流请求。
- 清空搜索热词，移除首页背景与商城底部标签，净化已知“我的”页面横幅和设备图片推广。
- 钱包、积分、彩铃默认保留，可在插件参数中分别隐藏。
- “我的页面推广”会清空两个目标接口的整个对象型 `data`，可能隐藏同模块的其他信息；若页面缺内容，优先关闭该开关。
- 参数只控制对应复写；域名拦截始终随插件启用。要完全恢复，请停用整个插件。

## 验证与限制

接口来自 ddgksf2013 公开的 V1.0.4 规则（标注更新于 2025-12-11），并非对当前联通 App 抓包所得。已收紧接口匹配、添加 HTTP 状态及 JSON 结构保护。不会主动匹配登录、余额查询、充值或支付路径，但不能据此保证所有 App 版本与业务流程均不受影响。

本地校验可检查 URL 正则和 JQ 行为，不能替代 Loon 导入与真机验证。启用后检查：冷启动广告、首页、我的页面，以及登录、余额查询和充值入口。查看 Loon 日志确认是否有解析错误、目标请求是否命中复写。不要仅为验证插件执行实际付款。

已缓存的广告可能继续显示；可先使用 App 内清理缓存功能并重启。没有加入未经核实的新开屏接口，因此不承诺所有开屏广告均被移除。若仍有广告，后续需要对应请求 URL 与去除个人信息后的响应样例继续适配。

## 来源

- [Loon 官方新版 Rewrite 文档](https://nsloon.app/docs/Rewrite/rewrite_v2/)：语法、JQ、参数和最低版本，核对于 2026-09-07。
- [Loon 官方插件文档](https://nsloon.app/docs/Plugin/)：元数据与插件结构。
- [ddgksf2013 中国联通规则](https://raw.githubusercontent.com/ddgksf2013/Rewrite/refs/heads/master/AdBlock/ChinaUnicomAds.conf)：接口与目标字段参考，感谢原作者。本文件是另行编写的 Loon 适配，并非原作者官方发布版本。

## 1.1.0：商城与权益底栏配置实验

用户提供的客户端 13.0000 抓包确认，fuInter 的 HomeFusion.bottomLabel 中，bottomMallKey 和 bottomWealthKey 分别包含商城与权益的标题、图标与链接。新增默认开启的“移除商城权益配置（实验）”，删除这两个键，保留其他字段。

尚未验证能否隐藏原生导航：样本中商城的 isLoad 已为字符串 0，不能视为隐藏开关。旧规则已删除商城配置，本次主要补全权益配置移除。App 可能回退内置底栏，不能承诺变为三个按钮。

更新插件后，确认实验开关开启，彻底退出并重新打开联通验证。如无效果可关闭实验开关；要同时恢复旧规则删除的商城配置，还需关闭“净化首页装饰”。

其余三份响应未确认主底栏数量控制字段，未作修改。原始抓包与账号信息不提交仓库。
