# 淘宝基础净化 · 导航适配未完成

[插件文件](./Taobao.Clean.Loon.plugin)，版本 0.1.0。

## 当前状态

用户要求的目标：底部移除视频；顶部仅保留关注、推荐、国补；净化其他推广。

**当前尚未实现两项导航修改**。截图只能确定界面文案，不能确定接口、配置字段、频道 ID 或客户端是否使用内置导航。不能把基础推广拦截当作导航精简已经完成。

目前默认拦截已知首页二楼活动和菜鸟广告接口。首页 newface 配置屏蔽作为实验选项，默认关闭。没有修改商品推荐流、关注、国补、购物车、消息、订单和支付接口。截图中的消费券悬浮条也未定位，不能保证移除。

## 安装

使用 Loon 3.5.1 (978) 或更高版本，导入插件，启用复写和 MitM，并安装、信任 Loon CA 证书。各功能可单独关闭。未实机验证；如首页加载异常请先停用插件。

## 完成导航适配需要的材料

1. 淘宝 App 版本号。
2. 从彻底退出淘宝到重新启动并打开首页这一段的网络记录，包含响应 JSON；只提供请求 URL 不足以确定导航字段。
3. 优先查看 `guide-acs.m.taobao.com` 和 `acs.m.taobao.com` 中包含顶部频道名称或底部“视频”的配置响应。这是排查范围，不代表已确认导航由这些域名下发。

分享前删除请求中的 Cookie、Authorization、Token、签名，以及响应中的姓名、手机号、地址、用户 ID、订单等个人信息。只需导航相关结构，保留频道名称、模块 ID、数组结构和字段名。若只有加密数据或完全找不到配置，需要进一步确认是否为客户端内置，不能保证 Loon 能隐藏。

拿到真实结构后再添加独立的“移除视频”和“顶部频道精简”开关、异常结构保留逻辑及测试。国补未下发时不能通过虚构一个标题创建可用频道。

## 依据与验证

- [Loon 新版 Rewrite 文档](https://nsloon.app/docs/Rewrite/rewrite_v2/)
- [公开淘宝插件中的接口记录](https://github.com/zmjde/loon/blob/main/Plugin/Taobao.plugin)：来自 2024 年规则，当前版本覆盖率未经验证。

本地仅检查 URL 匹配边界和配置声明，不代表 Loon 原生解析或淘宝实机验证。
